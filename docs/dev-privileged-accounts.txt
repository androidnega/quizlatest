QuizSnap — dev only (from seeders). Change these before production.

STAFF LOGIN — required URL: /admin_login
Do not use the student page (/login) for admin or manuel; staff credentials only work on the staff portal.

Use “Email or username” in the form. Passwords are the values in the table below.

Seeded admins (dev): both `admin` and `manuel` have `is_super_admin` = true, so both see “Manage users” and the same staff-directory powers. (In production, turn off `is_super_admin` for accounts that should not have that access.)

Super admin is defined only by `is_super_admin` on the `users` row. Same flag ⇒ same abilities; one admin UI at /dashboard after login.

Seeded `admin` password is stored via the same bcrypt path as `manuel` (query builder in OwnerAdminSeeder for manuel). If login ever fails after pulling code, run: php artisan db:seed --class=OwnerAdminSeeder

Role          Username                         Password
admin         admin                            admin123
admin         manuel                           admin123
coordinator   kofi.mensah@university.edu       admin123


STUDENT LOGIN (/login)
Use index number as login. Password is the same for every seeded student.

Role          Username (index number)          Password
student       BCS/<year>/001                   student123
student       BCS/<year>/002                   student123
student       BCS/<year>/003                   student123
student       BCS/<year>/004                   student123
student       BCS/<year>/005                   student123

Replace <year> with the year your database got from the seed (for example 2026).

Names for those five students: Akua Serwaa, Yaw Boateng, Efua Baidoo, Kwesi Annan, Abena Dapaah.
